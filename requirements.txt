# FerrePro Backend Dependencies
# For deployment on Vercel + Turso SQLite

flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
requests>=2.31.0
openpyxl>=3.1.0
gunicorn>=21.2.0

# Optional: libsql-client for Turso (native driver)
# Uncomment if you install it:
# libsql-client>=0.1.0